import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('Smoke test', (tester) async {
    // В проекте нет счетчика из шаблона Flutter, поэтому старый тест удалён/заменён.
    expect(true, isTrue);
  });
}
